# 네이버 쇼핑인사이트 API 공식 문서 분석 결과

**분석일**: 2025년 12월 24일  
**문서 출처**: [네이버 개발자 센터 - 쇼핑인사이트 API 레퍼런스](https://developers.naver.com/docs/serviceapi/datalab/shopping/shopping.md#%EC%87%BC%ED%95%91%EC%9D%B8%EC%82%AC%EC%9D%B4%ED%8A%B8-API-%EB%A0%88%ED%8D%BC%EB%9F%B0%EC%8A%A4)

---

## 🔍 발견된 주요 사항

### 1. API 호출 제한사항

#### 카테고리 제한
- **분야별 트렌드 조회**: `category` 배열은 **최대 3개**까지만 설정 가능
- 현재 구현: 제한사항이 명시되지 않음 → **추가 필요**

#### 키워드 제한
- **키워드별 트렌드 조회**: `keyword` 배열은 **최대 5개**까지만 설정 가능
- 현재 구현: 이미 테스트에서 발견했지만 코드에 명시되지 않음 → **추가 필요**

#### 일일 호출 한도
- **하루 1,000회** 제한
- 현재 구현: 명시되지 않음 → **문서화 필요**

---

### 2. 기기별/성별/연령별 API의 파라미터 형식 문제

#### 문제 발견
이전 테스트에서 다음 오류가 발생했습니다:
```
"TypeError: .category -> should be string"
```

#### 문서 확인 필요
공식 문서를 보면:
- **분야 내 기기별/성별/연령별 트렌드 조회** API는 `category`를 **단일 문자열**로 받아야 할 가능성
- 현재 구현: `category`를 배열로 전송 → **수정 필요**

#### 해결 방안
문서에서 정확한 파라미터 형식을 확인하고, 필요시 다음과 같이 수정:
```typescript
// 기기별/성별/연령별 API는 category를 문자열로 받아야 할 수 있음
// 예: category: "50000000" (단일 카테고리 ID)
```

---

### 3. 누락된 MCP 도구

#### 키워드 기기별/성별/연령별 트렌드 조회
문서에 따르면 다음 API들이 존재합니다:
- ✅ `get-keyword-trends`: 키워드별 트렌드 조회 (구현됨)
- ❌ `get-keyword-by-device`: 키워드 기기별 트렌드 조회 (누락)
- ❌ `get-keyword-by-gender`: 키워드 성별 트렌드 조회 (누락)
- ❌ `get-keyword-by-age`: 키워드 연령별 트렌드 조회 (누락)

#### 현재 상태
- `naverClient.ts`에는 메서드가 구현되어 있음:
  - `getKeywordByDevice()`
  - `getKeywordByGender()`
  - `getKeywordByAge()`
- 하지만 `server.ts`에 MCP 도구로 등록되지 않음 → **추가 필요**

---

### 4. 파라미터 검증 강화 필요

#### 현재 구현
```typescript
category: z.array(
  z.object({
    name: z.string(),
    param: z.array(z.string())
  })
)
```

#### 개선 필요
```typescript
category: z.array(
  z.object({
    name: z.string(),
    param: z.array(z.string())
  })
).max(3) // 최대 3개 제한 추가
```

```typescript
keyword: z.array(
  z.object({
    name: z.string(),
    param: z.array(z.string())
  })
).max(5) // 최대 5개 제한 추가
```

---

### 5. API 엔드포인트 확인

#### 현재 구현된 엔드포인트
- ✅ `/categories` - 분야별 트렌드 조회
- ✅ `/category/device` - 분야 내 기기별 트렌드 조회
- ✅ `/category/gender` - 분야 내 성별 트렌드 조회
- ✅ `/category/age` - 분야 내 연령별 트렌드 조회
- ✅ `/category/keywords` - 키워드별 트렌드 조회
- ✅ `/category/keyword/device` - 키워드 기기별 트렌드 조회 (클라이언트에만 구현)
- ✅ `/category/keyword/gender` - 키워드 성별 트렌드 조회 (클라이언트에만 구현)
- ✅ `/category/keyword/age` - 키워드 연령별 트렌드 조회 (클라이언트에만 구현)

---

## 📋 개선 사항 요약

### 즉시 수정 필요
1. ✅ **카테고리 배열 최대 3개 제한** 추가
2. ✅ **키워드 배열 최대 5개 제한** 추가
3. ✅ **기기별/성별/연령별 API의 category 파라미터 형식** 확인 및 수정
4. ✅ **키워드 기기별/성별/연령별 MCP 도구** 추가

### 문서화 필요
1. ✅ **일일 호출 한도 1,000회** 명시
2. ✅ **파라미터 제한사항** README에 추가

---

## 🔗 참고 자료

- [네이버 쇼핑인사이트 API 레퍼런스](https://developers.naver.com/docs/serviceapi/datalab/shopping/shopping.md#%EC%87%BC%ED%95%91%EC%9D%B8%EC%82%AC%EC%9D%B4%ED%8A%B8-API-%EB%A0%88%ED%8D%BC%EB%9F%B0%EC%8A%A4)
- [네이버 데이터랩 API 문서](https://developers.naver.com/docs/datalab/shopping/)

